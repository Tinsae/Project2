<apex:page standardController="Symptom__c" recordSetVar="symptoms" extensions="symptomExtension">
    <!-- Begin Default Content REMOVE THIS -->
    <h1>Congratulations</h1>
    This is your new Page
    <!-- End Default Content REMOVE THIS -->
    <apex:form>
        <apex:pageBlock title="{!userContact.Name} - {!userContact.Age}/{!userContact.Sex}">
                <apex:pageBlockButtons >
                    <apex:commandButton value="Process Symptoms"/>
                </apex:pageBlockButtons>
                <apex:pageBlockTable value="{!symptoms}" var="s">
                    <apex:column>
                        <apex:inputCheckbox/>
                    </apex:column>
                    <apex:column value="{!s.Name}"/>
                    <apex:column value="{!s.InfermedicaId__c}"/>
                    <apex:column value="{!s.Name}"/>
                </apex:pageBlockTable>
            </apex:pageBlock>
    </apex:form>
</apex:page>
